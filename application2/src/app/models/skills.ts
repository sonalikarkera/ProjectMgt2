export class Skills {

    skillId: number;
    skillName: string;

    constructor(skillId ?: number, skillName ?: string) {
        this.skillId = skillId;
        this.skillName = skillName;
    }
}